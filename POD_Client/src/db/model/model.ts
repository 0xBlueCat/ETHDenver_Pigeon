export interface IEnvelope {
  address: string;
  status: number;
  height: number;
  txHash: string;
  txStatus: string;
}

export interface IRSS3Follow {
  address: string;
  status: number;
  height: number;
  txHash: string;
  txStatus: string;
}
